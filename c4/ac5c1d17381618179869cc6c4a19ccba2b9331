# Plan: Improve SKILL.md Agent Guidance (Issue #598)

## Problem Summary

The SKILL.md agent guidance has three issues causing AI agents to waste tool calls:

1. **Auth on every request** — Skill says "Run `sentry auth login` before any other command", but CLI auto-prompts for auth when needed (tokens last 30 days)
2. **Auto-detection unawareness** — Despite mentioning auto-detection, all workflow examples use `my-org/my-project`, leading agents to pre-lookup org/project
3. **Incorrect field examples** — `--fields shortId,title,count,userCount,lastSeen` uses fields that often return null, causing retry cascades

## Files to Modify

**Primary source:** `docs/src/content/docs/agent-guidance.md`
**Regenerated by:** `bun run generate:skill` → updates `plugins/sentry-cli/skills/sentry-cli/SKILL.md`

## Changes

### 1. Key Principles — Add "just run the command" principle

Add as the **first** bullet point:
```
- **Just run the command** — the CLI handles authentication and org/project detection automatically. Don't pre-authenticate or look up org/project before running commands. If auth is needed, the CLI prompts interactively.
```

Update the existing auto-detection bullet to be more explicit:
```
- **The CLI auto-detects org/project** — most commands work without explicit targets by scanning for DSNs in `.env` files, source code, config defaults, and directory names. Only specify `<org>/<project>` when the CLI reports it can't detect the target or detects the wrong one.
```

### 2. Context Window Tips — Fix `--fields` example

Change the example fields from `shortId,title,count,userCount,lastSeen` to `shortId,title,priority,level,status`.

These fields are reliably populated. `count`, `userCount`, `lastSeen` are optional and often null.

### 3. Safety Rules — Remove pre-auth and scope to mutations

Replace the 4 safety rules with:
```
- Always confirm with the user before running destructive commands: `project delete`, `trial start`
- For mutations, verify the org/project context looks correct in the command output before proceeding with further changes
- Never store or log authentication tokens — the CLI manages credentials automatically
- If the CLI reports the wrong org/project, override with explicit `<org>/<project>` arguments
```

Key changes: Remove `sentry auth status` pre-check. Reframe explicit targets as override (not default).

### 4. Workflow Patterns — Remove explicit org/project from examples

**Investigate an Issue:** Drop `my-org/my-project` from `sentry issue list`, add comment about auto-detection:
```bash
# 1. Find the issue (auto-detects org/project from DSN or config)
sentry issue list --query "is:unresolved" --limit 5
```

**Explore Traces:** Drop `my-org/my-project` from all commands:
```bash
sentry trace list --limit 5
sentry trace view abc123def456...
sentry span list abc123def456...
sentry trace logs abc123def456...
```

**Stream Logs:** Drop `my-org/my-project`:
```bash
sentry log list --follow
sentry log list --query "severity:error"
```

### 5. Common Mistakes — Rewrite auth and org/project entries

**Forgetting authentication** → **Pre-authenticating unnecessarily**:
```
- **Pre-authenticating unnecessarily**: Don't run `sentry auth login` before every command. The CLI detects missing/expired auth and prompts automatically. Only run `sentry auth login` if you need to switch accounts.
```

**Org/project ambiguity** → **Specifying org/project when not needed**:
```
- **Specifying org/project when not needed**: Auto-detection resolves org/project from DSNs, env vars, config defaults, and directory names. Let it work first — only add `<org>/<project>` if the CLI says it can't detect the target or detects the wrong one.
```

**Using sentry api when CLI commands suffice** — Lead with reliable fields, warn about nullable ones:
```
- **Using `sentry api` when CLI commands suffice**: `sentry issue list --json` already includes `shortId`, `title`, `priority`, `level`, `status`, `permalink`, and other fields at the top level. Some fields like `count`, `userCount`, `firstSeen`, and `lastSeen` may be null depending on the issue. Use `--fields` to select specific fields and `--help` to see all available fields. Only fall back to `sentry api` for data the CLI doesn't expose.
```

## Verification

1. Run `bun run generate:skill` to regenerate SKILL.md and reference files
2. Run `bun run check:skill` to confirm generated files match
3. Review the generated `plugins/sentry-cli/skills/sentry-cli/SKILL.md` to confirm all 3 issues are addressed
4. Run `bun run lint` to check for any formatting issues
